- name: Install and configure opencode
  hosts: localhost
  tasks:
    - name: Install opencode
      shell: curl -fsSL https://opencode.ai/install | bash
      args:
        creates: ~/.opencode/bin/opencode

    - name: Stow opencode configuration
      command: "stow -d ~/.dotfiles -t ~/ opencode"
