export LOCAL_DEV_CERT=${HOME}/dev/local-ssl/local.pendo.io.pem
export LOCAL_DEV_KEY=${HOME}/dev/local-ssl/local.pendo.io-key.pem

export PUPPETEER_SKIP_CHROMIUM_DOWNLOAD=true
export PUPPETEER_EXECUTABLE_PATH=`which chromium`

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/francis.gurr/google-cloud-sdk/path.zsh.inc' ]; then . '/Users/francis.gurr/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/francis.gurr/google-cloud-sdk/completion.zsh.inc' ]; then . '/Users/francis.gurr/google-cloud-sdk/completion.zsh.inc'; fi
