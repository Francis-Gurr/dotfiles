{{ if ne .chezmoi.os "darwin" -}}
#!/bin/sh
# Refresh the font cache after the FiraCode Nerd Font external is applied (Linux only;
# macOS auto-detects fonts in ~/Library/Fonts). os: {{ .chezmoi.os }}
set -eu
fc-cache -f "$HOME/.local/share/fonts"
{{ end -}}
