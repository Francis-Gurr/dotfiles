{
  "name": "dotfiles",
  "version": "0.0.0",
  "private": true,
  "description": "Commit linting for the dotfiles repo (see commitlint/README.md).",
  "devDependencies": {
    "@commitlint/cli": "^21.0.2",
    "@commitlint/config-conventional": "^21.0.2",
    "@commitlint/types": "^21.0.1"
  }
}
